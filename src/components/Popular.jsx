export default function Popular({ text, className }) {
  return (
    <section id="movie-section">
      <h2>
        <span className="page-title">{text}:</span> Play Trailler, Get Full list
        and Details by clicking on movies.
      </h2>
      {/*<div id="movies" />*/}
    </section>
  );
}
